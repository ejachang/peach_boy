const avatars = {
  avatar: [
    './img-ref/avatar-boy.png',
    './img-ref/avatar-girl.png',
    './img-ref/avatar-grill.png'
  ]
};

const defaultAvatar = Object.freeze({
  avatar: girl
});

const avatarReducer = () => {
  if (action.type === 'GIRL') {

  } else if (action.type === 'BOY') {
    
  } else if (action.type === 'GIRL') {

  } 
  return state;
};

export default avatarReducer; 